#include <stdio.h>
#include <stdlib.h>

int main()
{char c;
    printf(" enter a character\n");
    fflush(stdin);
    fflush(stdout);
scanf("%c",&c);
printf("ascii value of %c =%d",c,c);

    return 0;
}
